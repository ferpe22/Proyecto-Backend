<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

<header class="userProfile">
    {{#unless admin}}
    <a href="/profile" class="bold">Profile</a>
    {{/unless}}
    <div>
        <p>Bienvenido/a <span class="bold">{{user.name}}</span>!</p>
        {{#if user.lastname}} <p><span class="bold">Apellido: </span>{{user.lastname}}</p>{{/if}}
        <p><span class="bold">Edad: </span>{{user.age}}</p>
        {{#if user.email}} <p><span class="bold">Email: </span>{{user.email}}</p> {{/if}}
        <p><span class="bold">Role: </span>{{user.role}}</p>
    </div>
    <a href="/logout" class="logout">Logout</a>
</header>

<section class="homeSection">
    <h1>VENDEMOS TODO</h1>
    <img src="" alt="Descripción de la imagen">
    <div>
        {{#unless admin}}
        <a href="/products" class="bold">🔗 Ver Productos</a>
        <a href="/chat" class="bold">🔗 Chat</a>
        {{/unless}}
        {{#if admin}}
        <a href="/realtimeproducts" class="bold">🔗 Inventario</a>
        {{/if}}
    </div>
</section>

{{!-- <section class="container">
    <h2>HOME</h2>
    <p> </p>
    <div class="table-responsive">
        <table class="table table-bordered table-md justify-middle">
        <thead class="table-ligth">
            <tr>
                <th class="text-center">ID</th>
                <th class="text-center">Titulo</th>
                <th class="text-center">Descripción</th>
                <th class="text-center">Codigo</th>
                <th class="text-center">Categoria</th>
                <th class="text-center">Stock</th>
                <th class="text-center">Precio</th>
            </tr>
        </thead>
        <tbody>
            {{#each products}}
            <tr>
                <td class="text-center">{{this._id}}</td>
                <td class="text-center">{{this.title}}</td>
                <td class="text-center">{{this.description}}</td>
                <td class="text-center">{{this.code}}</td>
                <td class="text-center">{{this.category}}</td>
                <td class="text-center">{{this.stock}}</td>
                <td class="text-center">{{this.price}}</td> 
            </tr>
            {{/each}}
        </tbody>
        </table>
        <div class="container">
            {{#if productsDB.hasPrevPage}}
            <a href="{{generatePaginationLink productsDB.prevPage}}">Pagina anterior</a>
            {{/if}}
            <p>Pagina {{productsDB.page}} de {{productsDB.totalPages}}</p>
            {{#if productsDB.hasNextPage}}
            <a href="{{generatePaginationLink productsDB.nextPage}}">Pagina siguiente</a>
            {{/if}}
        </div>
    </div>
</section> --}}


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>